using System;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using DataModel;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Main.ViewModel;

namespace Tests
{
    [TestClass]
    public class SearchViewModelTests
    {
        [TestMethod]
        public void Test()
        {
            //var context = new MainEntititesContext();
            //var roomsRepository = new RoomsRepository(context);
            //var searchvm = new RoomsSearchViewModel(roomsRepository);
            //var room = new Rooms() { RoomNum = "Test" };
            //roomsRepository.Add(room);
            //searchvm.SearchItem = "RoomNum";
            //searchvm.SearchWord = "Test";
            //Assert.AreEqual(1, roomsRepository.Collection.Count, "Error!");
            //Assert.AreSame(roomsRepository.Collection, roomsRepository.Collection, "FER");
            //roomsRepository.Remove(room);
        }
    }
}
